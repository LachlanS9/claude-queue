---
repo: api
model: sonnet
thinking: none
base-branch: main
vars:
  Feature: "What to build — be specific (e.g. 'user authentication endpoint', 'CSV export for invoices')"
---

Implement {{Feature}} in this codebase.

Follow the existing patterns and conventions you find. Keep changes focused — do not refactor unrelated code. Write tests if the project has a test suite.

Be specific about what you build and where. If the task is ambiguous, make a reasonable decision and implement it fully rather than asking for clarification.
